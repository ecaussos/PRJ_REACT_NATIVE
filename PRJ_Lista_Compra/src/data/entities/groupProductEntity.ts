// src/data/entities/groupProductEntity.ts
export interface GroupProductEntity {
  id_group: number;   // ID gerado automaticamente (INTEGER)
  nm_group: string;   // Nome do grupo (ex: Mercearia, Hortifrúti, etc.)
}